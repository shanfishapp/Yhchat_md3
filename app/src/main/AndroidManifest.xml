<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- 音频播放权限 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    
    <!-- 文件下载权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- 存储权限用于保存图片和文件 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <!-- 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- 相机权限用于二维码扫描 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".CanaryApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YhchatCanary">
        <!-- 聊天界面 Activity -->
        <activity
            android:name=".ui.chat.ChatActivity"
            android:exported="false"
            android:label="聊天"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.YhchatCanary" />
        <activity
            android:name=".ui.group.GroupInfoActivity"
            android:exported="false"
            android:label="群聊信息"
            android:theme="@style/Theme.YhchatCanary" />
        <activity
            android:name=".ui.create.CreateActivity"
            android:exported="false"
            android:label="创建"
            android:theme="@style/Theme.YhchatCanary" />
        <activity
            android:name=".ui.group.GroupSettingsActivity"
            android:exported="false"
            android:label="群聊设置"
            android:theme="@style/Theme.YhchatCanary" />
        
        <activity
            android:name=".ui.group.GroupTagManagementActivity"
            android:exported="false"
            android:label="标签管理"
            android:theme="@style/Theme.YhchatCanary" />
        
        <activity
            android:name=".ui.group.GroupTagDetailActivity"
            android:exported="false"
            android:label="标签详情"
            android:theme="@style/Theme.YhchatCanary" />
            
        <activity
            android:name=".ui.group.GroupMembersActivity"
            android:exported="false"
            android:label="成员列表"
            android:theme="@style/Theme.YhchatCanary" />

        <activity
            android:name=".ui.group.GroupBotManagementActivity"
            android:exported="false"
            android:label="机器人管理"
            android:theme="@style/Theme.YhchatCanary" />
        
        <activity
            android:name=".ui.group.ChatSearchActivity"
            android:exported="false"
            android:label="群聊历史记录搜索"
            android:theme="@style/Theme.YhchatCanary" />
        
        <activity
            android:name=".ui.community.MyPostsActivity"
            android:exported="false"
            android:label="我的文章"
            android:theme="@style/Theme.YhchatCanary" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.YhchatCanary">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- 分区详情Activity -->
        <activity
            android:name=".ui.community.BoardDetailActivity"
            android:exported="false"
            android:label="云湖社区"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 创建文章Activity -->
        <activity
            android:name=".ui.community.CreatePostActivity"
            android:exported="false"
            android:label="发布文章"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 草稿箱Activity -->
        <activity
            android:name=".ui.community.DraftBoxActivity"
            android:exported="false"
            android:label="草稿箱"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 文章详情Activity -->
        <activity
            android:name=".ui.community.PostDetailActivity"
            android:exported="true"
            android:label="文章详情"
            android:theme="@style/Theme.YhchatCanary">
            
            <!-- 处理yunhu://深度链接 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="yunhu" 
                      android:host="post-detail" />
            </intent-filter>
        </activity>
            
        <!-- WebView Activity -->
        <activity
            android:name=".ui.webview.WebViewActivity"
            android:exported="false"
            android:label="网页"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 设置 Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:label="设置"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- HTML 设置 Activity -->
        <activity
            android:name=".ui.settings.HtmlSettingsActivity"
            android:exported="false"
            android:label="HTML 设置"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 导航栏设置 Activity -->
        <activity
            android:name=".ui.settings.NavigationSettingsActivity"
            android:exported="false"
            android:label="导航栏管理"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 应用详情 Activity -->
        <activity
            android:name=".ui.settings.AppInfoActivity"
            android:exported="false"
            android:label="应用详情"
            android:theme="@style/Theme.YhchatCanary" />

        <!-- 用户资料 Activity -->
        <activity
            android:name=".ui.profile.UserProfileActivity"
            android:exported="false"
            android:label="用户资料"
            android:theme="@style/Theme.YhchatCanary" />
        
        <!-- 用户详情 Activity -->
        <activity
            android:name=".ui.user.UserDetailActivity"
            android:exported="false"
            android:label="用户详情"
            android:theme="@style/Theme.YhchatCanary" />
        
        <!-- 表情包详情 -->
        <activity
            android:name=".ui.sticker.StickerPackDetailActivity"
            android:exported="false"
            android:label="表情包详情"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 群聊列表 Activity -->
        <activity
            android:name=".ui.community.GroupListActivity"
            android:exported="false"
            android:label="群聊列表"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 聊天添加 Activity -->
        <activity
            android:name=".ui.chat.ChatAddActivity"
            android:exported="true"
            android:label="添加聊天"
            android:theme="@style/Theme.YhchatCanary">
            
            <!-- 处理yunhu://chat-add深度链接 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="yunhu" 
                      android:host="chat-add" />
                <data android:scheme="https" 
                      android:host="yhfx.jwznb.com" />
                <data android:scheme="yunhu" 
                      android:host="jwznb.com" />
            </intent-filter>
        </activity>
            
        <!-- 搜索 Activity -->
        <activity
            android:name=".ui.community.SearchActivity"
            android:exported="false"
            android:label="搜索"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 屏蔽用户列表 Activity -->
        <activity
            android:name=".ui.community.BlockedUsersActivity"
            android:exported="false"
            android:label="屏蔽用户"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 在线设备 Activity -->
        <activity
            android:name=".ui.settings.OnlineDevicesActivity"
            android:exported="false"
            android:label="在线设备"
            android:theme="@style/Theme.YhchatCanary" />
        <activity
            android:name=".ui.settings.ChangePasswordActivity"
            android:exported="false"
            android:label="更改密码"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 机器人信息 Activity -->
        <activity
            android:name=".ui.bot.BotInfoActivity"
            android:exported="false"
            android:label="机器人信息"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 机器人详细信息 Activity -->
        <activity
            android:name=".ui.bot.BotDetailActivity"
            android:exported="false"
            android:label="机器人详细信息"
            android:theme="@style/Theme.YhchatCanary" />
        
        <!-- 机器人管理 Activity -->
        <activity
            android:name=".ui.bot.BotManagementActivity"
            android:exported="false"
            android:label="机器人管理"
            android:theme="@style/Theme.YhchatCanary" />
        
        <!-- 机器人编辑 Activity -->
        <activity
            android:name=".ui.bot.BotEditActivity"
            android:exported="false"
            android:label="编辑机器人"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 金币商城 Activity -->
        <activity
            android:name=".ui.coin.CoinShopActivity"
            android:exported="false"
            android:label="金币商城"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 任务中心 Activity -->
        <activity
            android:name=".ui.coin.CoinDetailActivity"
            android:exported="false"
            android:label="任务中心"
            android:theme="@style/Theme.YhchatCanary" />
        
        <!-- 金币明细 Activity -->
        <activity
            android:name=".ui.coin.CoinRecordActivity"
            android:exported="false"
            android:label="金币明细"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 用户统计 Activity -->
        <activity
            android:name=".ui.stats.UserStatsActivity"
            android:exported="false"
            android:label="云湖用户统计"
            android:theme="@style/Theme.YhchatCanary" />

        <activity
            android:name=".ui.coin.ProductDetailActivity"
            android:exported="false"
            android:label="商品详情"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 聊天背景设置 Activity -->
        <activity
            android:name=".ui.background.ChatBackgroundActivity"
            android:exported="false"
            android:label="聊天背景"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 群网盘 Activity -->
        <activity
            android:name=".ui.disk.GroupDiskActivity"
            android:exported="false"
            android:label="群网盘"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- WebDAV 浏览器 Activity -->
        <activity
            android:name=".ui.disk.webdav.WebDAVBrowserActivity"
            android:exported="false"
            android:label="WebDAV"
            android:theme="@style/Theme.YhchatCanary" />
       
       <!-- WebDAV 设置 Activity -->
        <activity
            android:name=".ui.disk.webdav.WebDAVSettingsActivity"
            android:exported="false"
            android:label="WebDAV 设置"
            android:theme="@style/Theme.YhchatCanary" />

        <!-- WebDAV 下载列表 Activity -->
        <activity
            android:name=".ui.disk.webdav.WebDAVDownloadListActivity"
            android:exported="false"
            android:label="下载列表"
            android:theme="@style/Theme.YhchatCanary" />
        
        <!-- 综合搜索 Activity -->

        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="false"
            android:label="搜索"
            android:theme="@style/Theme.YhchatCanary" />

        <activity
            android:name=".ui.search.ComprehensiveSearchActivity"
            android:exported="false"
            android:label="综合搜索"
            android:theme="@style/Theme.YhchatCanary" />

        <activity
            android:name=".ui.community.EditPostActivity"
            android:exported="false"
            android:label="编辑文章"
            android:theme="@style/Theme.YhchatCanary" />

        <activity
            android:name=".ui.bot.BotSettingsActivity"
            android:exported="false"
            android:label="编辑机器人"
            android:theme="@style/Theme.YhchatCanary" />
            
        <activity
            android:name=".ui.bot.InstructionManagementActivity"
            android:exported="false"
            android:label="指令管理"
            android:theme="@style/Theme.YhchatCanary" />
            
        <activity
            android:name=".ui.bot.InstructionEditActivity"
            android:exported="false"
            android:label="编辑指令"
            android:theme="@style/Theme.YhchatCanary" />
            
        <activity
            android:name=".ui.bot.InstructionPreviewActivity"
            android:exported="false"
            android:label="指令预览"
            android:theme="@style/Theme.YhchatCanary" />

        <activity
            android:name=".ui.discover.DiscoverGroupsActivity"
            android:exported="false"
            android:label="发现群聊"
            android:theme="@style/Theme.YhchatCanary" />
            
        <activity
            android:name=".ui.discover.BotListActivity"
            android:exported="false"
            android:label="机器人商店"
            android:theme="@style/Theme.YhchatCanary" />
            
        <activity
            android:name=".ui.discover.BotDetailActivity"
            android:exported="false"
            android:label="机器人详情"
            android:theme="@style/Theme.YhchatCanary" />

        <!-- 二维码扫描 Activity -->
        <activity
            android:name=".ui.qrcode.QRCodeScannerActivity"
            android:exported="false"
            android:label="扫描二维码"
            android:theme="@style/Theme.YhchatCanary" />
            
        <!-- 语音播放服务 -->
        <service
            android:name=".service.AudioPlayerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
            
        <!-- 文件下载服务 -->
        <service
            android:name=".service.FileDownloadService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
            
        <!-- FileProvider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>